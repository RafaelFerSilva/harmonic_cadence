# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Poetry
poetry.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.venv
env/
venv/
ENV/

# Tests
.coverage
htmlcov/
.pytest_cache/
report_json/
report_html/
report_markdown/
data/


.agent/
.agents/
.claude/
.gemini/
base_estudo/
.ruff_cache/

# Cifras locais (substrato do local-chord-input) — fora da fronteira de copyright,
# não versionadas (coerente com "não armazenar cifras", só fatos no corpus).
cifras/

# Corpus persistido (persist-analysis-corpus) — artefato DERIVADO do motor,
# regenerável via `harmonic corpus build`; nunca é fonte de verdade.
*.duckdb
*.duckdb.wal
songbook-bossa-nova-v2.md
bossa-nova-songbook-v3-v3.md
songbook-v4-v2.md

# Relatório de corpus (corpus-analytics) — derivado do corpus gitignored,
# regenerável via `harmonic corpus report`.
corpus-report.md

# Worklist de anomalia (Camada C) — derivada do corpus gitignored (títulos +
# acordes), regenerável via `harmonic corpus anomalies`.
anomaly-worklist.md

# Songbooks fonte (PDF, copyright) — NUNCA versionar; só os FATOS derivados
# (regra + análise) entram no repo.
songbooks/
